# Python
__pycache__/
*.pyc

# Virtual environment
venv/

# Raw dataset (large; regeneratable from the zips, or exclude the zips too if
# storing data elsewhere)
sensor_data/
label_data/
*.zip

# All generated plots (every plots/ folder is produced by a build script --
# paper_threshold_validation/plots, analysis_3panel/plots, analysis_4panel/plots,
# rolling_regression/plots -- and any future ones).
**/plots/

# Large generated interactive HTML dashboards (regenerable from their build
# scripts; each embeds every trial, tens of MB). Remove a line if you want that
# one version-controlled.
fall_pattern_analysis/signal_quality/signal_dashboard.html
fall_pattern_analysis/signal_quality/signal_quality.html
fall_pattern_analysis/rolling_regression/beta_dashboard.html

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
